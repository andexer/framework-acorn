import { defineConfig } from 'vite';
import tailwind from '@tailwindcss/vite';
import laravel from 'laravel-vite-plugin';

export default defineConfig({
    base: '/wp-content/plugins/{{slug}}/public/build/',
    plugins: [
        tailwind(),
        laravel({
            input: [
                'resources/css/app.css',
                'resources/js/app.js',
            ],
            refresh: true,
        }),
    ],
});
